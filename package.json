{
  "name": "melon-api",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22.15"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test:routes": "node --import ./test/register-typescript.mjs --test test/*.test.mjs",
    "test": "pnpm typecheck && pnpm test:routes"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260621.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.48.0"
  }
}
